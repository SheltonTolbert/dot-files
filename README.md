# **dot-files:** 
    This is mostly for me, but it's for you too

## installation

    the setup script installs a bunch of programs and dependencies that make up the tools of my dev environment
    stuff like tmux, neovim, node, oh-my-zsh, and runescape
    it also downloads a nerd font, which you'll have to set yourself

- clone the repo `git clone https://github.com/SheltonTolbert/dot-files.git ~/.dotfiles`
- install deps and setup sym-links `~/.dotfiles/install`

---

#### keeping up to date

- run `~/.dotfiles/update` to update sym-links. this skips the dependency installation step

## Usage

### Tmux
#### Tmuxinator
#### [SketchyBar](https://github.com/FelixKratz/SketchyBar?tab=readme-ov-file)


#### Plugins
### Neovim
### Aerospace

#### Normal Mode
- `alt + /`: toggle horizontal/vertaccordion layouaccordion layout
- `alt + ,`: toggle accordion layout
- `alt + +`: increase pane size
- `alt + -`: decrease pane size
- `alt + shift + <arrow>`: Join two windows

##### Navigation

#### Service Mode
- `alt + shift + ;`: enter service mode
    - `r`: reset
    - `esc`: reload config

#### Custom Modes
